---
export interface Props {
	to: string;

}

import { GITHUB_REPOSITORY } from '../consts.ts';
const { to } = Astro.props;
---

<a href={`${GITHUB_REPOSITORY}/tree/master/packages/${to}`} target="_blank"><slot /></a>
