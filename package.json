{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "reinitialize": "npm run clean && npm run bootstrap",
    "test": "lerna run test",
    "clean": "lerna run clean && lerna clean -y",
    "build": "lerna run build && lerna run lint && lerna run test",
    "version": "lerna version",
    "publish": "lerna publish from-git",
    "ls": "lerna ls"
  },
  "devDependencies": {
    "lerna": "^3.13.0"
  }
}
